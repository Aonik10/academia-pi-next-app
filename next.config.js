/** @type {import('next').NextConfig} */
const nextConfig = {};

module.exports = nextConfig;

/*

const nextConfig = {
    redirects: async () => {
        return [
            {
                source: "/signin",
                destination: "/",
                permanent: false,
            },
        ];
    },
};

*/
